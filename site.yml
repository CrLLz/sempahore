---
- name: Remove apt lock file
  file:
    state: absent
    path: "/var/lib/dpkg/lock-frontend"
  become: true
  tags: apt
  
- hosts: all
  tasks:
    - name: Print OS info
      ansible.builtin.package:
        name: apache2
