---
- hosts: all
  tasks:
    - name: Update all packages to the latest version
      apt:
        upgrade: full
      become: true
      tags: apt
    - name: Print OS info
      ansible.builtin.package:
        name: apache2
